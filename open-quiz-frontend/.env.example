# Leave empty when using the Vite development proxy or the provided
# same-origin Caddy deployment. Set an absolute backend origin only when the
# frontend and API are served separately.
VITE_API_URL=
