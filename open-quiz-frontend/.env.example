# Leave empty when using the Vite development proxy or the provided
# same-origin Caddy deployment. Set an absolute backend origin only when the
# frontend and API are served separately. Pass the same value as the
# VITE_API_URL Docker build argument so Caddy permits the API in its CSP.
VITE_API_URL=
